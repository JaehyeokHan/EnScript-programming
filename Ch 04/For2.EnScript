class MainClass{
   void GetChar (String &string) {
      for(int i=0; i < string.GetLength(); i++) {
         Console.Write(¡°{0}¡±, string[i]);
      }
   }
   void Main(CaseClass c) {
       SystemClass::ClearConsole(SystemClass::SHOWCONSOLE);
       String str = ¡°Enjoy EnScript Programing!!!¡±;
       GetChar(str);
   }
}